#[derive(Debug)]
pub struct List {
    pub id: i32,
    pub title: String,
    pub description: String,
}

#[derive(Debug)]
pub struct Item {
    pub id: i32,
    pub title: String,
    pub description: String,
}
